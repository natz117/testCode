/*
    There is a string (s) of lowercase English letters 
    that is repeated infinitely many times. 

    Given an integer (i), find and print the number of letter a's 
    if the string were repeated to a length of (i)
Example:
    s = 'abcac'
    i = 10
    The substring we consider is 'abcacabcac', 
    the first characters of the infinite string. 
    There are 4 occurrences of a in the substring.
*/
