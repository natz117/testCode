/**
 * Given a string N (number string), the task is to
 * count the minimum number of digits
 * to be removed from N to make it
 * divisible by d (number).
 * if N is not divisible by d log the
 * string 'unable to find divisible number'
 */
